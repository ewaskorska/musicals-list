import React from "react";
import ReactDOM from "react-dom";
import Setup from "./components/ReactRouterSetup/ReactRouterSetup";

ReactDOM.render(
  <React.StrictMode>
    <Setup />
  </React.StrictMode>,
  document.getElementById("root")
);
